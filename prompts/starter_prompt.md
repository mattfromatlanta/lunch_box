Project goal:

Create a macOS application that will read audio files from a target folder and convert them to 16-bit 44.1khz wav files with a specific naming convention that we will detail later. Details will be worked out as we go.

We will use the JUCE framework for audio processing.

Initial goal: Do a status check on basic dependencies available for C++ development and verify.

Step 1:

Verify the essential dependencies for C++ are installed and check the JUCE version installed on this machine. Do not install anything new at this time. Assemble a list of what may be missing.

Step 2:

Create a hello_world inside this folder (chompi_pack) that can successfully compile